# M1-02 设计说明（src/native 静态库占位）

1. 在顶层 `CMakeLists.txt` 中启用 `src/native` 子目录，作为 C++ 运行时统一入口。
2. 新增 `src/native/CMakeLists.txt`，集中注册 `core`、`net`、`ipc`、`host` 四个子模块。
3. 每个子模块独立 CMake 配置，生成同名 `STATIC` 目标，便于后续扩展。
4. 为每个静态库放置最小占位 `.cpp` 文件，确保编译和链接流程可用。
5. 暂时将模块目录作为公共头文件包含路径，后续再拆分 include 结构。