# M1-01 设计说明

**目标**  
建立顶层 CMake 构建入口与 Windows/Linux 工具链占位文件，为后续模块接入提供统一入口。

**设计要点**
1. 顶层 `CMakeLists.txt` 只做最小初始化（版本、工程名、语言、标准）。
2. 预留 `cmake/` 模块路径，便于后续集中管理构建选项与平台差异。
3. 提供 Windows/Linux 工具链占位文件，仅包含最小标识与用法注释。
4. 暂不引入任何子目录或目标，避免与后续模块未就绪冲突。
5. 为后续扩展预留基础选项开关（如测试、工具）。

