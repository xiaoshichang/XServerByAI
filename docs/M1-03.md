# M1-03 设计说明（进程入口可执行占位）

1. 在 `src/native` 下为 `gm`、`gate`、`game` 增加子目录 CMake 配置，生成对应可执行目标。
2. 每个可执行目标提供独立的最小 `main` 源文件，返回 0 作为占位实现。
3. 入口目标链接 `core`/`net`/`ipc`/`host` 静态库，确保后续依赖路径可用。
4. 在 `src/native/CMakeLists.txt` 中统一注册三个入口子目录。
5. 保持命名与目录结构一致，便于后续扩展启动参数与生命周期逻辑。