# 开发计划（修订版）

本计划基于当前项目约束：架构允许多机多进程部署，开发期默认单机多进程测试，C++ 运行时 + C# 业务，`nethost` 互操作，客户端 KCP，节点间 ZeroMQ over TCP，应用层统一二进制消息体协议。
业务层采用“分布式实体架构”。

**分布式实体架构要点**
1. `ServerEntity`
负责属性维护、状态同步与持久化的实体基类，例如玩家实体、场景（`Space`）、NPC 等；可进一步划分为可迁移与不可迁移两类。
2. `ServerStubEntity`
继承自 `ServerEntity`，用于集群内全局服务（例如匹配、聊天、排行榜等）；其承载 Game 在启动时确定，运行期不迁移。

**状态说明**
1. 状态字段取值：未开发 / 开发中 / 已完成。
2. 当前默认全部为“未开发”，后续按实际进度逐条更新。

**里程碑结构**
共 6 个里程碑，合计约 98 条开发条目。条目包含依赖关系（`Deps`），用于约束实现顺序。

**里程碑 M1：工程骨架与规范（16 条）**
1. M1-01 创建顶层 CMake 与工具链配置占位（Windows/Linux）。Deps: 无 | 状态: 已完成
2. M1-02 创建 `src/native` 静态库目标 `core`/`net`/`ipc`/`host` 占位。Deps: M1-01 | 状态: 已完成
3. M1-03 创建统一进程入口可执行目标 `xserver-node` 占位。Deps: M1-01 | 状态: 已完成
4. M1-04 创建 `src/managed` 解决方案与项目占位（Foundation/GameLogic）。Deps: 无 | 状态: 已完成
5. M1-05 约定目录结构、命名与命名空间规则（文档）。Deps: 无 | 状态: 已完成
6. M1-06 定义二进制协议包头结构与常量。Deps: 无 | 状态: 已完成
7. M1-07 定义 msgId 分段与命名规范（文档）。Deps: M1-06 | 状态: 已完成
8. M1-08 定义错误码范围与编码规则（文档）。Deps: M1-06 | 状态: 已完成
9. M1-09 定义进程注册与心跳消息结构（文档）。Deps: M1-06 | 状态: 已完成
10. M1-10 定义 Gate↔Game 消息封装格式（文档）。Deps: M1-06 | 状态: 已完成
11. M1-11 定义 KCP 参数配置项与默认值（文档）。Deps: 无 | 状态: 已完成
12. M1-12 定义会话与路由数据模型（文档）。Deps: M1-09 | 状态: 已完成
13. M1-13 接入基础第三方依赖（`spdlog` / `zeromq` / standalone `asio` / header-only `nlohmann/json` vendoring 与 CMake 集成）。Deps: M1-01, M1-02 | 状态: 已完成
14. M1-14 定义分布式实体架构概念与职责（文档）。Deps: 无 | 状态: 已完成
15. M1-15 明确 C++↔C# 互操作入口签名与约定（文档）。Deps: M1-02, M1-04 | 状态: 已完成
16. M1-16 定义配置与日志输出规范（文档）。Deps: 无 | 状态: 已完成

**里程碑 M2：C++ 基础运行时（13 条）**
1. M2-01 基于 `spdlog` 实现跨平台日志模块（文件输出、等级过滤）。Deps: M1-13, M1-16 | 状态: 已完成
2. M2-02 实现配置加载（按规范 JSON）。Deps: M1-16 | 状态: 已完成
3. M2-03 基于 standalone `asio::steady_timer` 实现基础时间工具与定时器封装。Deps: M1-13 | 状态: 已完成
4. M2-04 基于 standalone `asio` 执行器实现基础运行线程封装（线程名、启动/停止）。Deps: M1-13 | 状态: 已完成
5. M2-05 基于 standalone `asio::io_context` 实现事件循环骨架（主线程）。Deps: M1-13, M2-03, M2-04 | 状态: 已完成
6. M2-06 实现网络字节序工具与序列化基础函数。Deps: M1-06 | 状态: 已完成
7. M2-07 基于 `zeromq` 封装节点间主动连接端（ZeroMQ over TCP）。Deps: M1-13, M2-06 | 状态: 已完成
8. M2-08 基于 `zeromq` 封装节点间监听端（ZeroMQ over TCP）。Deps: M1-13, M2-06 | 状态: 已完成
9. M2-09 实现统一消息封包与解析逻辑（复用 `PacketHeader`）。Deps: M1-13, M2-06 | 状态: 已完成
10. M2-10 实现消息分发器（按 msgId）。Deps: M2-09 | 状态: 已完成
11. M2-11 实现心跳消息编解码。Deps: M1-09, M2-06 | 状态: 已完成
12. M2-12 实现注册消息编解码。Deps: M1-09, M2-06 | 状态: 已完成
13. M2-14 实现基础 metrics 结构（连接数、吞吐）。Deps: M2-08 | 状态: 已完成

**里程碑 M3：进程与注册控制面（17 条）**
1. M3-01 实现统一 `xserver-node` 运行时入口与节点角色分派（根据启动参数确定 `processType` 与 `nodeId`）。Deps: M1-03, M2-05 | 状态: 已完成
2. M3-02 实现 GM 角色生命周期与内部控制端口监听（ZeroMQ over TCP）。Deps: M3-01, M2-08 | 状态: 已完成
3. M3-03 实现注册表数据结构（保存 Gate/Game 节点信息与 NodeID）。Deps: M1-09 | 状态: 已完成
4. M3-04 实现注册请求处理与响应。Deps: M2-12, M3-03 | 状态: 已完成
5. M3-05 实现心跳处理与超时检测。Deps: M2-11, M2-03 | 状态: 未开发
6. M3-06 实现 GM→Gate/Game 配置与集群就绪状态下发消息。Deps: M2-06 | 状态: 未开发
7. M3-07 实现 GM 管理命令接口（本地 CLI 或 HTTP）。Deps: M3-02 | 状态: 未开发
8. M3-08 实现 Gate 角色启动骨架与 GM 控制链路接入。Deps: M3-01, M2-07 | 状态: 未开发
9. M3-09 实现 Gate→GM 注册与心跳。Deps: M3-08, M2-11, M2-12 | 状态: 未开发
10. M3-10 实现 Game 角色启动骨架与 GM 控制链路接入。Deps: M3-01, M2-07 | 状态: 未开发
11. M3-11 实现 Game→GM 注册与心跳。Deps: M3-10, M2-11, M2-12 | 状态: 未开发
12. M3-12 实现 GM 推送 Game 节点路由目录到 Gate。Deps: M3-03, M3-06 | 状态: 未开发
13. M3-13 实现 Gate 维护 Game 节点路由目录与变更更新。Deps: M3-12 | 状态: 未开发
14. M3-14 实现 Game 服务就绪状态上报（含 ServerStubEntity ready 聚合）。Deps: M3-03 | 状态: 未开发
15. M3-15 实现 GM 断线剔除、服务就绪聚合与路由更新通知。Deps: M3-05, M3-12, M3-14 | 状态: 未开发
16. M3-16 完成集成测试：通过 `xserver-node` 启动 GM + Gate/Game 注册心跳。Deps: M3-09, M3-11 | 状态: 未开发
17. M3-17 管理命令：查询在线进程与强制下线。Deps: M3-07, M3-15 | 状态: 未开发

**里程碑 M4：网络与会话（16 条）**
1. M4-01 集成 KCP 库并封装会话对象。Deps: M2-06 | 状态: 未开发
2. M4-02 实现 Gate KCP 监听骨架、受控开放与会话创建；仅在全部 ServerStubEntity ready 后开放客户端入口。Deps: M4-01, M3-08, M3-06, M3-15 | 状态: 未开发
3. M4-03 实现客户端鉴权流程骨架（占位）。Deps: M4-02 | 状态: 未开发
4. M4-04 实现 Gate 会话管理表（sessionId → playerId）。Deps: M4-02 | 状态: 未开发
5. M4-05 实现 Gate 与全部 Game 节点的内部 ZeroMQ over TCP 全连接池。Deps: M3-13, M2-07 | 状态: 未开发
6. M4-06 实现 Gate→Game 请求转发（含 seq）。Deps: M2-09, M2-10, M4-05 | 状态: 未开发
7. M4-07 实现 Game→Gate 响应回传。Deps: M2-09, M2-10, M4-05 | 状态: 未开发
8. M4-08 实现 Game→Gate 推送消息通道。Deps: M4-05 | 状态: 未开发
9. M4-09 实现 Gate→Client 推送通道（KCP）。Deps: M4-02 | 状态: 未开发
10. M4-10 实现会话断开清理与 Game 通知。Deps: M4-04, M4-06 | 状态: 未开发
11. M4-11 实现路由策略：固定绑定到首次分配 Game 节点。Deps: M3-13, M4-04 | 状态: 未开发
12. M4-12 实现简单负载感知分配（按连接数）。Deps: M3-14, M4-11 | 状态: 未开发
13. M4-13 实现内部 RPC 超时与重试策略。Deps: M2-03, M4-06 | 状态: 未开发
14. M4-14 完成协议兼容性测试（包头版本/flags）。Deps: M1-06, M2-09 | 状态: 未开发
15. M4-15 实现客户端心跳与超时踢出。Deps: M4-02 | 状态: 未开发
16. M4-16 完成断线重连流程（占位）。Deps: M4-15 | 状态: 未开发

**里程碑 M5：C# 分布式实体框架（17 条）**
1. M5-01 实现 `nethost` 初始化与 CLR 加载（Windows/Linux）。Deps: M1-15 | 状态: 未开发
2. M5-02 实现 C++→C# 入口绑定（函数指针获取）。Deps: M5-01 | 状态: 未开发
3. M5-03 建立 C# Framework 基础项目（ServerEntity/ServerStubEntity）。Deps: M1-04, M1-14 | 状态: 未开发
4. M5-04 实现 `ServerEntity` 基类（Id/属性/生命周期）。Deps: M5-03 | 状态: 未开发
5. M5-05 实现 `ServerStubEntity` 基类（全局服务语义）。Deps: M5-04 | 状态: 未开发
6. M5-06 实现实体注册表与查找索引。Deps: M5-04 | 状态: 未开发
7. M5-07 实现实体消息分发（msgId → entity）。Deps: M2-10, M5-06 | 状态: 未开发
8. M5-08 实现实体路由：session → player proxy → space mailbox。Deps: M1-12, M4-04 | 状态: 未开发
9. M5-09 实现场景实体模型（SpaceEntity/PlayerEntity）。Deps: M5-04, M5-08 | 状态: 未开发
10. M5-10 实现实体 Tick 接口与驱动（C++ 调用）。Deps: M5-02, M5-04 | 状态: 未开发
11. M5-11 实现状态变更脏标记与同步钩子。Deps: M5-04 | 状态: 未开发
12. M5-12 实现持久化接口（异步保存/加载）。Deps: M5-04 | 状态: 未开发
13. M5-13 实现内存型持久化适配器（占位）。Deps: M5-12 | 状态: 未开发
14. M5-14 实现 Stub 示例：MatchService。Deps: M5-05 | 状态: 未开发
15. M5-15 实现 Stub 示例：ChatService。Deps: M5-05 | 状态: 未开发
16. M5-16 实现 C# 侧业务错误码映射。Deps: M1-08, M5-07 | 状态: 未开发
17. M5-17 实现 C# 日志桥接到 C++ 日志。Deps: M2-01, M5-02 | 状态: 未开发

**里程碑 M6：闭环集成与质量（17 条）**
1. M6-01 实现 C++↔C# blittable 数据结构约定与封送。Deps: M5-02 | 状态: 未开发
2. M6-02 实现 Game 进程调用 C# 入口（Init/OnMessage/OnTick）与本地 ServerStubEntity ready 判定。Deps: M5-02, M5-14, M5-15 | 状态: 未开发
3. M6-03 实现 Game 将业务响应回传 Gate。Deps: M4-07, M6-02 | 状态: 未开发
4. M6-04 实现 Gate 将业务响应回传客户端。Deps: M4-09, M6-03 | 状态: 未开发
5. M6-05 完成端到端测试：客户端→Gate→Game→C#，并验证全部 ServerStubEntity ready 前 Gate 不开放客户端入口。Deps: M6-04, M6-02 | 状态: 未开发
6. M6-06 实现进程配置文件（GM/Gate/Game）。Deps: M2-02, M3-01 | 状态: 未开发
7. M6-07 完成 Windows/Linux 构建脚本与说明（含 `3rd/` vendored 依赖）。Deps: M1-01, M1-04, M1-13 | 状态: 未开发
8. M6-08 完成一键启动脚本（GM/Gate/Game）。Deps: M3-01, M3-08, M3-10 | 状态: 未开发
9. M6-09 实现基础可观测性输出：日志/metrics 汇总。Deps: M2-14, M5-17 | 状态: 未开发
10. M6-10 完成稳定性测试：心跳超时/断线重连。Deps: M4-15, M4-16 | 状态: 未开发
11. M6-11 完成性能基线测试：连接数/吞吐/RTT。Deps: M4-02, M4-06, M2-14 | 状态: 未开发
12. M6-12 完成协议兼容性回归测试。Deps: M4-14 | 状态: 未开发
13. M6-13 完成崩溃恢复流程（占位）。Deps: M3-15 | 状态: 未开发
14. M6-14 完成路由一致性与会话迁移策略评估（占位）。Deps: M4-11 | 状态: 未开发
15. M6-15 编写运行手册与部署说明。Deps: M6-07, M6-08 | 状态: 未开发
16. M6-16 编写开发规范与贡献指南。Deps: M1-05 | 状态: 未开发
17. M6-17 梳理多机多进程部署验证项与风险清单。Deps: M3-03, M4-11 | 状态: 未开发

**说明**
1. 标注“占位”的条目表示先完成可运行骨架，后续再扩展实现。
2. 允许在实现过程中合并或拆分条目，但需保持依赖顺序。

